<%= form_with(model: contact) do |form| %>
  <% if contact.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(contact.errors.count, "error") %> prohibited this contact from being saved:</h2>

      <ul>
        <% contact.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :your_name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :email %>
    <%= form.email_field :email %>
  </div>

  <div class='cointainer'>
    <div class="field">
        <%= form.check_box :send_to_one %>
        <%= form.label :send_to_two, "lameira@ufpa.br" %>
    </div>

    <div class="field">
        <%= form.check_box :send_to_two %>
        <%= form.label :send_to_three, "anderson@ufpa.br" %>
    </div>

    <div class="field">
        <%= form.check_box :send_to_three %>
        <%= form.label :send_to_fout, "nahum@ufpa.br" %>
    </div>

    <div class="field">
        <%= form.check_box :send_to_four %>
        <%= form.label :send_to_one, "rogerio@ufpa.br" %>
    </div>
  </div>

  <div class="field">
    <%= form.label :Subject %>
    <%= form.text_field :subject %>
  </div>

  <div class="field">
    <%= form.label 'Message', class: 'label' %>
    <%= form.rich_text_area :message, value: "Send attachments at the end of the message" %>
  </div>

  <div class="field is-grouped">
    <div class="control">
      <%= form.submit 'Send email', class: 'button is-link' %>
    </div>
  </div>

<% end %>